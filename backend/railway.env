# Railway Production Environment Configuration
NODE_ENV=production

# PostgreSQL Database Configuration
# DATABASE_URL will be automatically set by Railway when you add a PostgreSQL service

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_SECRET="production-jwt-secret-change-this-to-256-bit-secure-key-min-32-characters"
JWT_REFRESH_SECRET="production-refresh-secret-change-this-to-256-bit-secure-key-min-32-characters"

# Server Configuration
PORT=3001
HOST=0.0.0.0

# CORS Configuration (Update with your actual frontend URL)
CORS_ORIGIN="https://your-frontend-domain.railway.app"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# Logging
LOG_LEVEL=warn

# Security
BCRYPT_ROUNDS=12
SESSION_TIMEOUT=3600000

# Database Connection Pool
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10
